<!--
ADAPTER PLAN FRAGMENTS — spliced into core/templates/feature-plan.core.md at its
`<!-- INJECT: adapter plan_fragments here -->` marker (inside §7 Cross-System Integration).

Add this project's DOMAIN integration points — the live-system hooks and subsystem wiring the
core cannot name. These appear either as additional §7 sub-headings or as new adapter sections
numbered §A1, §A2, … (no collision with core 1–11).

The core ships only "Bootstrap & Registration" and "Persistence" as universal §7 sub-headings;
everything domain-shaped (the systems this project's features must touch) comes from here.

Delete the example below and write your own.

Example (delete):

### <Live-System Hook Name>
<!-- e.g. how a feature plugs into this project's own subsystems / queues / pipelines, etc.
Name the concrete integration point and what the feature registers with it. -->
- 
-->
