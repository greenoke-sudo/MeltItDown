<!--
ADAPTER SPEC FRAGMENTS — spliced into core/templates/feature-spec.core.md at its
`<!-- INJECT: adapter spec_fragments here -->` marker.

Add domain-specific spec sections the core skeleton does not name. Number them §A1, §A2, …
so they never collide with the fixed core section numbers (1–11). Each section stays in
DESIGN LANGUAGE and obeys the same EARS + banned-token discipline as the core sections.

Delete the example below and write your own. If your project needs no extra spec sections,
leave this file empty (just the header comment) — the core spec template stands alone.

Example (delete):

## A1. <Domain Section Name>

<!-- e.g. platform/output-format considerations, domain-specific surfaces, etc. -->
- 
-->
