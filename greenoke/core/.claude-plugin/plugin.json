{
  "name": "greenoke",
  "description": "Greenoke — portable agentic engineering core: a project-agnostic spec → plan → build pipeline driven entirely by a per-project adapter manifest. Pointed at any adapter, the same core means the same thing.",
  "version": "0.2.0"
}
